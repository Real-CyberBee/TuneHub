# 无构建产物、无 node_modules —— 这个项目是零依赖的纯静态站点
.DS_Store
*.log
.chrome-profile/

# 发布暂存目录（deploy/deploy.sh 组装出来的）
.deploy/

# 部署密钥：只放本机，永远不入库
.env
.env.local
.env.deploy.local
deploy/.env.deploy.local
*.pem
*.key

# Python 部署脚本的字节码缓存
__pycache__/
*.pyc
